# Legacy Portfolio UI

This folder contains the previous Mido portfolio: dual-mode Client/Dev terminal interface with English/Arabic i18n.

It is **not imported** by the current app. Kept for reference only.

To restore it, wire `src/_legacy/components/Hero.jsx` back into App and re-enable i18n in `main.jsx`.
